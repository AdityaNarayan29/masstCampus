{
  "name": "school-crm-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant School CRM with custom domain support",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "db:migrate": "cd apps/backend && pnpm prisma migrate dev",
    "db:seed": "cd apps/backend && pnpm prisma db seed",
    "db:studio": "cd apps/backend && pnpm prisma studio",
    "db:push": "cd apps/backend && pnpm prisma db push",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.11.2",
    "prettier": "^3.1.1",
    "turbo": "^1.11.2",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.12.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
